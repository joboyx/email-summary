{
  "name": "email-summary",
  "version": "1.0.0",
  "main": "Code.js",
  "scripts": {
    "setup": "clasp login && clasp login --creds credentials.json",
    "start": "clasp push && clasp deploy && clasp run summarizeAndSendDailyEmail",
    "watch": "clasp logs --open",
    "deploy": "clasp push && clasp deploy"
  },
  "author": "",
  "license": "UNLICENSED",
  "description": ""
}
