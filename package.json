{
  "name": "email-summary",
  "version": "1.0.0",
  "main": "Code.js",
  "scripts": {
    "setup": "clasp login && clasp login --creds credentials.json",
    "start": "clasp push --force && clasp deploy && clasp run summarizeAndSendDailyEmail",
    "watch": "clasp logs --open",
    "deploy": "clasp push --force && clasp deploy"
  },
  "author": "",
  "license": "UNLICENSED",
  "description": ""
}
